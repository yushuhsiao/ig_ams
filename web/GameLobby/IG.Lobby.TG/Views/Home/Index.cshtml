<div class="logo"></div>
<div class="slogan"></div>
<div class="hr"></div>
<div class="games clearfix">
    <div class="game mahjong">
        <div class="cover"></div>
        <a class="enter" onclick="openTableWindow('@Url.Action("TaiwanMahjong", "Game")', 1280, 720)">Taiwan Mahjong</a>
    </div>
    <div class="game doudizhu">
        <div class="cover"></div>
        <a class="enter" onclick="openTableWindow('@Url.Action("DouDizhu", "Game")', 1280, 720)">Dou Dizhu</a>
    </div>
    <div class="game texasholdem">
        <div class="cover"></div>
        <a class="enter" onclick="openTableWindow('@Url.Action("TexasHoldem", "Game")', 1280, 720)">Texas Holdem</a>
    </div>
</div>
@section Scripts {
    <link href="~/assets/fancybox/jquery.fancybox.css" rel="stylesheet" />
    <script src="~/assets/fancybox/jquery.fancybox.min.js"></script>
    <script>
        function openTableWindow(url, width, height) {
            return window.open(url, '_blank', 'resizable=yes,location=no,menubar=no,scrollbars=no,status=no,titlebar=no,toolbar=no,width=' + width + ',height=' + height);
        }

        var lobby = null;

        function showPhotoCheckBox(url, width, height) {
            lobby = {
                url: url,
                width: width,
                height: height
            };

            $.fancybox({
                type: 'iframe',
                href: '@Url.Action("PhotoCheck", "Webcam")',
                fitToView: false,
                autoSize: false,
                closeBtn: false,
                width: 550,
                height: 550,
                padding: 0
            });
        }

        function verifyPhotoSuccess() {
            openNewWindow(lobby.url, lobby.width, lobby.height);
            $.fancybox.close();
            alertify.success('照片比對成功。');
        }

        function verifyPhotoFail() {
            alertify.error('照片比對失敗。');
        }
    </script>
}
