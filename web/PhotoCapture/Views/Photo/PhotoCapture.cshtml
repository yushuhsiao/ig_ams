@{ 
    Layout = "_Photo.cshtml";
    Page.Title = "PhotoCapture(註冊時拍照，提供未來比對的樣本)";
    ams.api_client.TakePictureUrls urls = Model;
}
    <script type="text/javascript">
        @*var swfUrl = '@String.Format("{0}/webcam/PhotoCapture.swf?v=201611011136", ConfigHelper.AssetServerUrl)';*@
        var swfUrl = '@urls.swfUrl';

        function takePictureDone() {
            alertify.success('已完成拍照。');
        }
    </script>
